# Шпаргалка: `stat`, `fstat`, `lstat` в C

Компактный справочник по получению метаданных файлов.

---

## 🔹 Три функции

| Функция | Аргумент | Что возвращает |
|---------|----------|----------------|
| `stat(path, &st)` | Путь к файлу | Информация о файле (по ссылке — о цели) |
| `fstat(fd, &st)` | Открытый дескриптор | Информация об открытом файле |
| `lstat(path, &st)` | Путь к файлу | Информация о самой ссылке (если это ссылка) |

**Заголовок:** `<sys/stat.h>`

**Возврат:** `0` — успех, `-1` — ошибка.

---

## 🔹 Шаблон

```c
#include <sys/stat.h>
#include <stdio.h>

struct stat st;

if (stat("file.txt", &st) == -1) {
    perror("stat");
    return 1;
}

printf("Размер: %ld\n", (long)st.st_size);
```

---

## 🔹 Поля `struct stat`

| Поле | Что содержит |
|------|--------------|
| `st_size` | Размер в байтах |
| `st_mode` | Тип + права |
| `st_nlink` | Число жёстких ссылок |
| `st_uid`, `st_gid` | Владелец и группа |
| `st_ino` | Номер inode |
| `st_dev` | Устройство |
| `st_atime` | Время доступа |
| `st_mtime` | Время изменения содержимого |
| `st_ctime` | Время изменения метаданных |

---

## 🔹 Тип файла — макросы

```c
S_ISREG(st.st_mode)    // обычный файл
S_ISDIR(st.st_mode)    // каталог
S_ISLNK(st.st_mode)    // символическая ссылка
S_ISCHR(st.st_mode)    // символьное устройство
S_ISBLK(st.st_mode)    // блочное устройство
S_ISFIFO(st.st_mode)   // канал (FIFO)
S_ISSOCK(st.st_mode)   // сокет
```

**Пример:**

```c
if (S_ISREG(st.st_mode))
    printf("Обычный файл\n");
else if (S_ISDIR(st.st_mode))
    printf("Каталог\n");
else if (S_ISLNK(st.st_mode))
    printf("Символическая ссылка\n");
```

---

## 🔹 Права доступа — биты

| Бит | Что означает |
|-----|--------------|
| `S_IRUSR` | Чтение владельцем |
| `S_IWUSR` | Запись владельцем |
| `S_IXUSR` | Выполнение владельцем |
| `S_IRGRP` | Чтение группой |
| `S_IWGRP` | Запись группой |
| `S_IXGRP` | Выполнение группой |
| `S_IROTH` | Чтение остальными |
| `S_IWOTH` | Запись остальными |
| `S_IXOTH` | Выполнение остальными |

**Проверка:**

```c
if (st.st_mode & S_IRUSR)
    printf("Владелец может читать\n");
```

**Восьмеричный вид:**

```c
printf("%o\n", st.st_mode & 0777);   // например: 644
```

---

## 🔹 Время

`time_t` — секунды с 1 января 1970 года.

**Преобразование в читаемый вид:**

```c
#include <time.h>

char buf[64];
struct tm *tm = localtime(&st.st_mtime);
strftime(buf, sizeof(buf), "%Y-%m-%d %H:%M:%S", tm);
printf("Изменён: %s\n", buf);
```

**Спецификаторы:**

| Код | Значение |
|-----|----------|
| `%Y` | Год |
| `%m` | Месяц |
| `%d` | День |
| `%H` | Часы |
| `%M` | Минуты |
| `%S` | Секунды |

---

## 🔹 Разница трёх функций

| Ситуация | `stat` | `lstat` | `fstat` |
|----------|:------:|:-------:|:-------:|
| Обычный файл | о файле | о файле | о файле |
| Символическая ссылка | о **цели** | о **ссылке** | о файле по `fd` |
| Каталог | о каталоге | о каталоге | о каталоге |
| Открытый файл | — | — | ✅ |

**Используйте `fstat`**, когда файл уже открыт (безопаснее — нет гонки).
**Используйте `lstat`**, когда нужно узнать про саму ссылку.

---

## 🔹 Полный пример

```c
#include <stdio.h>
#include <sys/stat.h>
#include <time.h>

int main(int argc, char *argv[])
{
    if (argc != 2) {
        fprintf(stderr, "Использование: %s <файл>\n", argv[0]);
        return 1;
    }

    struct stat st;
    if (stat(argv[1], &st) == -1) {
        perror("stat");
        return 1;
    }

    printf("Файл: %s\n", argv[1]);
    printf("Размер: %ld байт\n", (long)st.st_size);
    printf("Inode: %lu\n", (unsigned long)st.st_ino);
    printf("Ссылок: %lu\n", (unsigned long)st.st_nlink);
    printf("Права: %o\n", st.st_mode & 0777);

    printf("Тип: ");
    if (S_ISREG(st.st_mode)) printf("обычный файл\n");
    else if (S_ISDIR(st.st_mode)) printf("каталог\n");
    else if (S_ISLNK(st.st_mode)) printf("символическая ссылка\n");
    else printf("другое\n");

    char buf[64];
    struct tm *tm = localtime(&st.st_mtime);
    strftime(buf, sizeof(buf), "%Y-%m-%d %H:%M:%S", tm);
    printf("Изменён: %s\n", buf);

    return 0;
}
```

---

## 🔹 Связь с `FILE *` и `open`

| Что есть | Как получить `struct stat` |
|----------|---------------------------|
| Путь к файлу | `stat(path, &st)` |
| `FILE *f` | `fstat(fileno(f), &st)` |
| `int fd` | `fstat(fd, &st)` |
| Символическая ссылка | `lstat(path, &st)` |

---

## 🔹 Типичные ошибки

| Ошибка | Решение |
|--------|---------|
| Нет проверки `-1` | `if (stat(...) == -1) { perror(...); }` |
| Забыли `&st` | `stat(path, &st)` |
| Сравнение `st_mode == 0644` | `(st.st_mode & 0777) == 0644` |
| `stat` для ссылки | Использовать `lstat` |
| `%d` для `st_size` | Приводить к `long`, писать `%ld` |
| Не подключили `<sys/stat.h>` | Добавить заголовок |

---

## 🔹 Сводка

| Что нужно | Функция / макрос |
|-----------|------------------|
| Информация по пути | `stat` |
| Информация по дескриптору | `fstat` |
| Информация о ссылке | `lstat` |
| Обычный файл? | `S_ISREG` |
| Каталог? | `S_ISDIR` |
| Ссылка? | `S_ISLNK` |
| Права в восьмеричном | `st.st_mode & 0777` |
| Время в читаемом виде | `localtime` + `strftime` |

---

**Главное правило:** `stat` — по имени, `fstat` — по дескриптору, `lstat` — про ссылку. Всегда проверяйте `-1` и используйте макросы `S_IS*` — не сравнивайте `st_mode` напрямую с числами.

