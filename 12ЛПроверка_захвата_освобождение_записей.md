# Лекция: Проверка захвата и освобождение записей

**Дисциплина:** Основы программирования на языке Си  
**Тема:** `LOCK_SH`, `LOCK_EX`, `LOCK_UN`  
**Тип занятия:** лекция (2 ч)

---

## 1. Цели и задачи

**Цель:** разобраться, как проверять, занят ли файл другим процессом, и как правильно снимать блокировку.

**Задачи:**
- Освоить три основных флага `flock`: `LOCK_SH`, `LOCK_EX`, `LOCK_UN`.
- Понять, когда блокировка **ждёт**, а когда возвращает ошибку.
- Научиться проверять захват через `LOCK_NB`.
- Разобраться с освобождением записей.

**Компетенции:** ОК 01, ОК 02, ПК 1.1, ПК 1.2.

---

## 2. Три основных флага

Функция `flock` принимает один из трёх флагов (или их комбинацию):

| Флаг | Что делает | Кто может держать одновременно |
|------|-----------|-------------------------------|
| `LOCK_SH` | **Разделяемая** блокировка | Много процессов (только чтение) |
| `LOCK_EX` | **Исключительная** блокировка | Только один процесс |
| `LOCK_UN` | **Снять** блокировку | — |

**Логика:**

```
LOCK_SH  — «я читаю, другим тоже можно читать, но не писать»
LOCK_EX  — «я пишу, никто больше не должен ни читать, ни писать»
LOCK_UN  — «я закончил, снимаю блокировку»
```

---

## 3. Совместимость блокировок

Блокировки **совместимы** не всегда. Правило:

| Уже стоит | Запрашивается | Результат |
|-----------|---------------|-----------|
| — | `LOCK_SH` | ✅ Разрешено |
| — | `LOCK_EX` | ✅ Разрешено |
| `LOCK_SH` | `LOCK_SH` | ✅ Разрешено (читают вместе) |
| `LOCK_SH` | `LOCK_EX` | ❌ Заблокировано (ждём) |
| `LOCK_EX` | `LOCK_SH` | ❌ Заблокировано |
| `LOCK_EX` | `LOCK_EX` | ❌ Заблокировано |

**Мнемоника:** разделяемые блокировки **дружат** между собой. Исключительная — **враждует со всеми**, включая другие исключительные.

---

## 4. Проверка захвата: `LOCK_NB`

По умолчанию `flock` **ждёт**, пока блокировка не станет доступна. Если нужно **проверить** — используйте `LOCK_NB` (non-blocking).

```c
if (flock(fd, LOCK_EX | LOCK_NB) == -1) {
    if (errno == EWOULDBLOCK) {
        printf("Файл занят другим процессом\n");
    } else {
        perror("flock");
    }
}
```

**Что происходит:**

| Ситуация | Без `LOCK_NB` | С `LOCK_NB` |
|----------|---------------|-------------|
| Блокировка свободна | ✅ Захватывает | ✅ Захватывает |
| Блокировка занята | ⏳ Ждёт | ❌ Возвращает `-1`, `errno = EWOULDBLOCK` |

**Константы:**
- `EWOULDBLOCK` = `EAGAIN` — «файл занят».
- Устанавливается в `errno`.

⚠️ **Не путайте:** `LOCK_NB` — это **дополнительный флаг**, комбинируется через `|`:

```c
flock(fd, LOCK_EX | LOCK_NB);
flock(fd, LOCK_SH | LOCK_NB);
```

---

## 5. Освобождение: `LOCK_UN`

```c
flock(fd, LOCK_UN);
```

**Что делает:** снимает **все** блокировки, которые процесс держит на этом файле.

⚠️ **Блокировка снимается автоматически** при `close(fd)`. Но явное `LOCK_UN` делает код понятнее.

**Пример полного цикла:**

```c
int fd = open("file.txt", O_RDWR);

// 1. Захватить
if (flock(fd, LOCK_EX) == -1) {
    perror("flock");
    close(fd);
    return 1;
}

// 2. Работать
write(fd, "data", 4);

// 3. Снять
flock(fd, LOCK_UN);
close(fd);
```

---

## 6. Проверка блокировки без захвата

`flock` **не умеет** проверять блокировку без её захвата. Для проверки используется `fcntl` с командой `F_GETLK`:

```c
struct flock lck;
lck.l_type = F_WRLCK;
lck.l_whence = SEEK_SET;
lck.l_start = 0;
lck.l_len = 0;

if (fcntl(fd, F_GETLK, &lck) == -1) {
    perror("fcntl");
}

if (lck.l_type == F_UNLCK) {
    printf("Файл свободен\n");
} else {
    printf("Файл занят процессом %d\n", lck.l_pid);
}
```

- `F_GETLK` **не захватывает** блокировку — только сообщает, есть ли она.
- Если блокировка есть, `lck.l_pid` содержит PID процесса-владельца.
- Если нет — `lck.l_type` равен `F_UNLCK`.

---

## 7. Повторный захват

Процесс может **несколько раз** захватить блокировку на один и тот же файл. Блокировка **не снимается**, пока не будет снята столько же раз.

```c
flock(fd, LOCK_EX);   // 1-й раз
flock(fd, LOCK_EX);   // 2-й раз — блокировка остаётся

flock(fd, LOCK_UN);   // сняли 1 раз — блокировка ещё стоит
flock(fd, LOCK_UN);   // сняли 2 раз — теперь снята
```

⚠️ **`close(fd)` снимает все блокировки сразу**, независимо от того, сколько раз их захватывали.

---

## 8. Практический пример: два процесса

**Программа 1 (держит блокировку):**

```c
#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <unistd.h>
#include <sys/file.h>

int main(void)
{
    int fd = open("lock.txt", O_RDWR | O_CREAT, 0644);
    if (fd == -1) { perror("open"); return 1; }

    printf("Захватываю блокировку...\n");
    if (flock(fd, LOCK_EX) == -1) {
        perror("flock");
        close(fd);
        return 1;
    }

    printf("Блокировка захвачена. Жду 10 секунд...\n");
    sleep(10);

    printf("Отпускаю блокировку\n");
    flock(fd, LOCK_UN);
    close(fd);
    return 0;
}
```

**Программа 2 (проверяет и не ждёт):**

```c
#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <unistd.h>
#include <errno.h>
#include <sys/file.h>

int main(void)
{
    int fd = open("lock.txt", O_RDWR | O_CREAT, 0644);
    if (fd == -1) { perror("open"); return 1; }

    if (flock(fd, LOCK_EX | LOCK_NB) == -1) {
        if (errno == EWOULDBLOCK) {
            printf("Файл занят другим процессом\n");
        } else {
            perror("flock");
        }
        close(fd);
        return 1;
    }

    printf("Блокировка захвачена\n");
    flock(fd, LOCK_UN);
    close(fd);
    return 0;
}
```

**Поведение:**
- Запустили первую → она держит блокировку 10 секунд.
- Запустили вторую → она сразу сообщит «Файл занят».
- Если подождать 10 секунд и запустить вторую — она захватит блокировку.

---

## 9. Что происходит при завершении процесса

**Ядро автоматически снимает все блокировки** при завершении процесса:

- Обычное завершение (`exit`, `return`).
- Аварийное завершение (`abort`, `SIGKILL`).
- Закрытие дескриптора.

⚠️ Это **гарантия**: если процесс упал, его блокировки не останутся «висеть». Другие процессы смогут продолжить работу.

---

## 10. Проверка освобождения

Чтобы убедиться, что блокировка снята:

```c
// Снимаем
flock(fd, LOCK_UN);

// Пробуем захватить снова (неблокирующе)
if (flock(fd, LOCK_EX | LOCK_NB) == -1) {
    if (errno == EWOULDBLOCK) {
        printf("Блокировка всё ещё держится\n");
    }
} else {
    printf("Блокировка снята успешно\n");
    flock(fd, LOCK_UN);
}
```

---

## 11. `LOCK_SH` на практике

`LOCK_SH` используют для **чтения**, когда важно, чтобы никто не писал в это время:

```c
int fd = open("data.txt", O_RDONLY);

// Несколько читателей могут взять LOCK_SH одновременно
if (flock(fd, LOCK_SH) == -1) {
    perror("flock");
    close(fd);
    return 1;
}

// Читаем
char buf[1024];
ssize_t n = read(fd, buf, sizeof(buf));

flock(fd, LOCK_UN);
close(fd);
```

**Правило:**
- Читаешь — `LOCK_SH`.
- Пишешь — `LOCK_EX`.

---

## 12. Типичные ошибки

| Ошибка | Что не так | Решение |
|--------|-----------|---------|
| `flock(fd, LOCK_EX)` без `LOCK_NB`, когда нужно проверить | Процесс зависнет | Использовать `LOCK_NB` |
| Не проверили `errno` | Непонятно, почему `-1` | Проверять `EWOULDBLOCK` |
| Не сняли блокировку | Другие процессы ждут | `LOCK_UN` или `close` |
| Забыли `close(fd)` | Дескриптор утекает | Всегда закрывать |
| Используют `LOCK_UN` без блокировки | Ничего не делает | Не критично |
| `LOCK_SH` для записи | Логическая ошибка | Для записи — `LOCK_EX` |
| `flock` на NFS | Не работает | Использовать `fcntl` |

---

## 13. Итоги

- **`LOCK_SH`** — разделяемая блокировка (чтение).
- **`LOCK_EX`** — исключительная блокировка (запись).
- **`LOCK_UN`** — снять блокировку.
- **`LOCK_NB`** — не ждать, а вернуть ошибку.
- **`EWOULDBLOCK`** — «файл занят» (при `LOCK_NB`).
- **`F_GETLK`** — проверить блокировку без захвата.
- **Ядро снимает** все блокировки при завершении процесса.
- **Разделяемые дружат**, исключительная — враждует со всеми.

---

## 14. Вопросы для самоконтроля

1. Чем `LOCK_SH` отличается от `LOCK_EX`?
2. Что произойдёт, если запросить `LOCK_EX`, когда уже стоит `LOCK_SH`?
3. Что делает `LOCK_NB`?
4. Как проверить, что файл занят, не блокируя процесс?
5. Как снять блокировку?
6. Что произойдёт с блокировкой при завершении процесса?
7. Как проверить блокировку без её захвата?
8. Что такое `EWOULDBLOCK`?

---

## 15. Домашнее задание

1. Написать программу, которая через `flock` с `LOCK_EX | LOCK_NB` проверяет, занят ли файл.
2. Написать программу, которая держит блокировку 10 секунд.
3. Запустить обе программы параллельно и наблюдать результат.
4. Проверить через `F_GETLK`, какая блокировка стоит на файле.
5. Ответить на вопросы самоконтроля.

---

## 16. Литература

1. Стивенс У. «UNIX. Разработка сетевых приложений».
2. Лав Р. «Linux. Системное программирование». Глава 5.
3. `man 2 flock`, `man 2 fcntl`.

---
