# Лекция: Скрипты Bash. Выбор и циклы

**Дисциплина:** Основы программирования на языке Си  
**Тема:** `if`, `case`, `for`, `while` в Bash  
**Тип занятия:** лекция (2 ч)

---

## 1. Цели и задачи

**Цель:** научиться писать сценарии на Bash — с ветвлениями и циклами.

**Задачи:**
- Освоить условный оператор `if`.
- Освоить множественный выбор `case`.
- Освоить циклы `for`, `while`, `until`.
- Познакомиться с проверками файлов и строк.
- Научиться писать простые скрипты автоматизации.

**Компетенции:** ОК 01, ОК 02, ПК 1.1, ПК 1.2.

---

## 2. Что такое скрипт Bash

**Скрипт** — это файл с командами, которые выполняются последовательно.

**Создание:**

```bash
nano script.sh
```

**Содержимое:**

```bash
#!/bin/bash
echo "Привет, мир!"
```

**Первая строка — shebang** — указывает, какой интерпретатор использовать.

**Сделать исполняемым:**

```bash
chmod +x script.sh
./script.sh
```

Или запустить без прав:

```bash
bash script.sh
```

---

## 3. Переменные

```bash
name="Иван"
age=25
echo "Меня зовут $name, мне $age лет"
```

**Правила:**
- **Без пробелов** вокруг `=`: `name="Иван"` ✅, `name = "Иван"` ❌.
- Обращение — через `$`: `$name` или `${name}`.
- `${name}` надёжнее: `${name}world` не спутает с `$nameworld`.

**Специальные переменные:**

| Переменная | Что содержит |
|------------|--------------|
| `$0` | Имя скрипта |
| `$1`, `$2`, ... | Аргументы |
| `$#` | Число аргументов |
| `$@` | Все аргументы (по отдельности) |
| `$*` | Все аргументы (одной строкой) |
| `$?` | Код возврата последней команды |
| `$$` | PID текущего скрипта |

**Пример:**

```bash
#!/bin/bash
echo "Скрипт: $0"
echo "Первый аргумент: $1"
echo "Всего аргументов: $#"
echo "Все: $@"
```

---

## 4. Условный оператор `if`

### Простая форма

```bash
if [ условие ]; then
    команды
fi
```

⚠️ **Пробелы вокруг `[` и `]` обязательны.** `[` — это команда, а не просто скобка.

**Пример:**

```bash
#!/bin/bash
age=20

if [ $age -ge 18 ]; then
    echo "Совершеннолетний"
fi
```

### Полная форма

```bash
if [ условие ]; then
    команды1
elif [ другое_условие ]; then
    команды2
else
    команды3
fi
```

**Пример:**

```bash
#!/bin/bash
read -p "Введите число: " num

if [ $num -gt 0 ]; then
    echo "Положительное"
elif [ $num -lt 0 ]; then
    echo "Отрицательное"
else
    echo "Ноль"
fi
```

### Операторы сравнения

**Числа:**

| Оператор | Что означает |
|----------|--------------|
| `-eq` | Равно |
| `-ne` | Не равно |
| `-gt` | Больше |
| `-ge` | Больше или равно |
| `-lt` | Меньше |
| `-le` | Меньше или равно |

⚠️ **Для чисел** — `-eq`, `-gt`. **Для строк** — `=`, `!=`.

**Строки:**

| Оператор | Что означает |
|----------|--------------|
| `=` или `==` | Равно |
| `!=` | Не равно |
| `-z` | Пустая строка |
| `-n` | Непустая строка |

**Файлы:**

| Оператор | Что проверяет |
|----------|---------------|
| `-e file` | Существует |
| `-f file` | Обычный файл |
| `-d file` | Каталог |
| `-r file` | Читаемый |
| `-w file` | Записываемый |
| `-x file` | Исполняемый |
| `-s file` | Не пустой |

**Логические операторы:**

| Оператор | Что означает |
|----------|--------------|
| `&&` | И (and) |
| `\|\|` | ИЛИ (or) |
| `!` | НЕ (not) |

**Примеры:**

```bash
if [ -f "file.txt" ]; then
    echo "Файл существует"
fi

if [ -d "docs" ] && [ -w "docs" ]; then
    echo "Каталог docs есть и доступен для записи"
fi

if [ ! -z "$name" ]; then
    echo "Имя не пустое: $name"
fi
```

### Двойные скобки `[[ ]]`

`[[ ]]` — расширенная версия `[ ]`:
- Поддерживает регулярные выражения.
- Безопаснее с пустыми переменными.
- Можно использовать `&&`, `||` внутри.

```bash
if [[ $name == "Иван" ]]; then
    echo "Это Иван"
fi

if [[ $file == *.txt ]]; then
    echo "Это текстовый файл"
fi
```

---

## 5. Множественный выбор `case`

Когда вариантов много — удобнее `case`:

```bash
case "$переменная" in
    шаблон1)
        команды1
        ;;
    шаблон2)
        команды2
        ;;
    *)
        команды_по_умолчанию
        ;;
esac
```

**Пример:**

```bash
#!/bin/bash
read -p "Введите команду: " cmd

case "$cmd" in
    start)
        echo "Запуск..."
        ;;
    stop)
        echo "Остановка..."
        ;;
    restart)
        echo "Перезапуск..."
        ;;
    status|info)
        echo "Состояние: OK"
        ;;
    *)
        echo "Неизвестная команда: $cmd"
        ;;
esac
```

**Особенности:**
- `;;` — конец ветки (как `break`).
- `|` — несколько вариантов в одной ветке.
- `*` — «всё остальное».
- Работает с **glob-шаблонами**: `*.txt`, `file?`, `[abc]`.

**Пример с шаблонами:**

```bash
case "$filename" in
    *.txt) echo "Текстовый файл" ;;
    *.jpg|*.png) echo "Изображение" ;;
    *.c|*.h) echo "Исходник C" ;;
    *) echo "Неизвестный тип" ;;
esac
```

---

## 6. Цикл `for`

### Перебор списка

```bash
for переменная in список; do
    команды
done
```

**Пример:**

```bash
for fruit in яблоко банан вишня; do
    echo "Фрукт: $fruit"
done
```

### Перебор файлов

```bash
for file in *.txt; do
    echo "Обрабатываю: $file"
done
```

### Перебор чисел

```bash
for i in {1..10}; do
    echo "Число: $i"
done
```

### C-подобный синтаксис

```bash
for ((i = 0; i < 10; i++)); do
    echo "i = $i"
done
```

### Перебор аргументов

```bash
for arg in "$@"; do
    echo "Аргумент: $arg"
done
```

⚠️ **`$@` в кавычках** сохраняет аргументы с пробелами как единое целое. Без кавычек — разобьёт.

---

## 7. Цикл `while`

```bash
while [ условие ]; do
    команды
done
```

**Пример: счётчик**

```bash
i=1
while [ $i -le 5 ]; do
    echo "Итерация $i"
    ((i++))
done
```

### Чтение файла построчно

```bash
while IFS= read -r line; do
    echo "Строка: $line"
done < file.txt
```

⚠️ **`IFS=`** — не обрезать пробелы. **`-r`** — не обрабатывать `\` как escape.

### Бесконечный цикл с выходом

```bash
while true; do
    read -p "Введите 'quit' для выхода: " cmd
    if [ "$cmd" = "quit" ]; then
        break
    fi
    echo "Вы ввели: $cmd"
done
```

### `until` — цикл «пока не»

```bash
i=1
until [ $i -gt 5 ]; do
    echo "i = $i"
    ((i++))
done
```

Работает как `while`, но продолжается, **пока условие ложно**.

---

## 8. Управление циклами

| Команда | Что делает |
|---------|-----------|
| `break` | Выйти из цикла |
| `continue` | Перейти к следующей итерации |
| `exit N` | Выйти из скрипта с кодом N |

**Пример:**

```bash
for i in {1..10}; do
    if [ $i -eq 5 ]; then
        continue   # пропустить 5
    fi
    if [ $i -eq 8 ]; then
        break      # остановиться на 8
    fi
    echo "$i"
done
```

**Вывод:**
```
1
2
3
4
6
7
```

---

## 9. Арифметика в Bash

**Двойные круглые скобки:**

```bash
((a = 5 + 3))
((a++))
((b = a * 2))
echo $b
```

**Внешняя команда `expr`:**

```bash
sum=$(expr 5 + 3)
```

**Через `$(( ))`:**

```bash
a=$((5 + 3))
b=$((a * 2))
```

⚠️ **Bash работает только с целыми числами.** Для дробных — `bc` или `awk`.

---

## 10. Полный пример: скрипт-калькулятор

```bash
#!/bin/bash

echo "Простой калькулятор"
read -p "Первое число: " a
read -p "Второе число: " b
read -p "Операция (+, -, *, /): " op

case "$op" in
    +) result=$((a + b)) ;;
    -) result=$((a - b)) ;;
    \*) result=$((a * b)) ;;
    /)
        if [ $b -eq 0 ]; then
            echo "Деление на ноль!"
            exit 1
        fi
        result=$((a / b))
        ;;
    *)
        echo "Неизвестная операция"
        exit 1
        ;;
esac

echo "Результат: $result"
```

⚠️ **`*` нужно экранировать** (`\*`) — иначе shell раскроет его как glob.

---

## 11. Практический пример: перебор файлов

```bash
#!/bin/bash

# Подсчёт .c-файлов и строк в них
count=0
total_lines=0

for file in *.c; do
    if [ -f "$file" ]; then
        lines=$(wc -l < "$file")
        echo "$file: $lines строк"
        count=$((count + 1))
        total_lines=$((total_lines + lines))
    fi
done

echo "Всего файлов: $count"
echo "Всего строк: $total_lines"
```

---

## 12. Типичные ошибки

| Ошибка | Что не так | Решение |
|--------|-----------|---------|
| `if [ $x = 5 ]` для чисел | Не сработает для `10` | `-eq` для чисел |
| Нет пробелов в `[ $x -eq 5 ]` | Ошибка синтаксиса | Пробелы обязательны |
| `if [$x -eq 5]` | `[` — это команда | Пробелы после `[` и перед `]` |
| `name = "Иван"` | Пробелы вокруг `=` | `name="Иван"` |
| `for i in 1..10` | Это не диапазон | `{1..10}` |
| `$@` без кавычек | Разбивает аргументы | `"$@"` |
| `*` в `case` без экранирования | Shell раскроет | `\*` или кавычки |
| `while read` без `IFS=` | Обрезает пробелы | `IFS= read -r` |

---

## 13. Итоги

- **`if`** — с пробелами вокруг `[ ]`.
- **Для чисел** — `-eq`, `-gt`, `-lt`; **для строк** — `=`, `!=`.
- **`[[ ]]`** — расширенная версия `[ ]`.
- **`case`** — множественный выбор, поддерживает glob.
- **`for`** — по списку, диапазону, файлам, C-стилю.
- **`while`** / **`until`** — циклы с условием.
- **`break`** / **`continue`** — управление.
- **`$@`** — всегда в кавычках.

---

## 14. Вопросы для самоконтроля

1. Зачем нужен `#!/bin/bash`?
2. Чем `[ ]` отличается от `[[ ]]`?
3. Как сравнить два числа в Bash?
4. Как сравнить две строки?
5. Чем `case` удобнее `if`?
6. Как перебрать файлы в цикле?
7. Что делает `IFS=` в `while read`?
8. Чем `while` отличается от `until`?
9. Что делает `break` и `continue`?
10. Как проверить, существует ли файл?

---

## 15. Домашнее задание

1. Написать скрипт, который приветствует пользователя по имени (из аргумента).
2. Написать скрипт с `case`: start/stop/restart/status.
3. Написать скрипт, считающий количество `.c` файлов в каталоге.
4. Написать цикл, выводящий числа от 1 до 100, пропуская кратные 3.
5. Написать скрипт-калькулятор с обработкой деления на ноль.
6. Ответить на вопросы самоконтроля.

---

## 16. Литература

1. Лав Р. «Linux. Системное программирование».
2. Колисниченко Д. «Linux. Полное руководство».
3. `man bash`, `help if`, `help case`, `help for`.
4. Онлайн: Bash Guide, tldr.

---

