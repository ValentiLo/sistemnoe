# Шпаргалка по арифметике указателей в C

Компактный справочник — можно распечатать и держать под рукой.

---

## 🔹 Основы

| Операция | Что означает |
|----------|--------------|
| `&x` | Адрес переменной `x` |
| `*p` | Значение по адресу `p` (разыменование) |
| `p = &x` | Присвоить указателю адрес `x` |
| `*p = 5` | Записать 5 по адресу `p` |
| `int *p` | Объявление указателя на `int` |
| `int **pp` | Указатель на указатель на `int` |

---

## 🔹 Шаг при арифметике

`p++` увеличивает адрес не на 1, а на **`sizeof(тип)`**.

| Тип указателя | Шаг при `p++` |
|---------------|:-------------:|
| `char *` | 1 байт |
| `short *` | 2 байта |
| `int *` | 4 байта |
| `long *` | 4 или 8 байт |
| `double *` | 8 байт |
| `void *` | ⚠️ запрещено (размер неизвестен) |

**Пример:**

```c
int a[5] = {10, 20, 30, 40, 50};
int *p = a;        // p → a[0]
p++;               // p → a[1] (адрес +4)
p += 2;            // p → a[3] (адрес +8)
```

---

## 🔹 Операции

| Операция | Что делает | Пример |
|----------|-----------|--------|
| `p + n` | Сдвиг вперёд на `n` элементов | `p + 2` |
| `p - n` | Сдвиг назад на `n` элементов | `p - 1` |
| `p++`, `p--` | Сдвиг на 1 элемент | `p++` |
| `p1 - p2` | **Количество элементов** между указателями | `&a[4] - &a[1] = 3` |
| `p1 == p2` | Сравнение адресов | `if (p == q)` |
| `p1 < p2` | Сравнение адресов (в одном массиве) | `if (p < q)` |
| `*p`, `p[0]` | Одно и то же | `*p` = `p[0]` |
| `*(p + n)`, `p[n]` | Одно и то же | `*(p+2)` = `p[2]` |

⚠️ **`p1 + p2`, `p1 * p2`, `p1 / p2`** — **запрещены**. Нельзя складывать два адреса.

---

## 🔹 Связь с массивами

```c
int a[5] = {10, 20, 30, 40, 50};
int *p = a;            // p = &a[0]
```

| Запись | Что означает | Значение |
|--------|--------------|----------|
| `a[0]` | Первый элемент | 10 |
| `*a` | То же самое | 10 |
| `*(a+1)` | Второй элемент | 20 |
| `a[1]` | То же самое | 20 |
| `*(a+i)` | Элемент с индексом i | `a[i]` |
| `p[i]` | То же, что `a[i]` | — |

**Правило:** `a[i]` ≡ `*(a + i)`.

---

## 🔹 Указатели и строки

```c
char s[] = "Hello";
char *p = s;      // p → 'H'
```

| Операция | Результат |
|----------|-----------|
| `*p` | `'H'` |
| `*(p+1)` | `'e'` |
| `p[1]` | `'e'` |
| `p += 2` | `p` → `'l'` |
| `*p` после сдвига | `'l'` |

---

## 🔹 Инкремент и декремент

```c
int x = 5;
int *p = &x;

(*p)++;      // x = 6  (увеличиваем значение)
*p++;        // ⚠️ ошибка приоритета: сначала p++, потом *
             // p сдвигается, а не значение!
```

**Разбор:**

| Запись | Что происходит |
|--------|----------------|
| `(*p)++` | Увеличить **значение** по адресу `p` |
| `*p++` | Сначала сдвинуть указатель, потом разыменовать (старое значение) |
| `*++p` | Сначала сдвинуть, потом разыменовать (новое значение) |
| `++*p` | Увеличить значение по адресу |

**Совет:** всегда ставьте скобки — `(*p)++`.

---

## 🔹 Указатели и функции

| Передача | Что происходит |
|----------|----------------|
| `void f(int x)` | Передаётся **копия** — оригинал не изменится |
| `void f(int *p)` | Передаётся **адрес** — можно изменить оригинал |
| `void f(int **pp)` | Можно изменить **сам указатель** |

**Пример:**

```c
void inc(int *p) { (*p)++; }        // изменит оригинал
void set_null(int **pp) { *pp = NULL; }   // изменит указатель
```

---

## 🔹 Указатели на указатели

```c
int x = 42;
int *p = &x;
int **pp = &p;

**pp = 100;      // изменяет x
*pp = NULL;      // изменяет p
```

| Выражение | Значение |
|-----------|----------|
| `pp` | Адрес `p` |
| `*pp` | Значение `p` (адрес `x`) |
| `**pp` | Значение `x` |

---

## 🔹 Разность указателей

```c
int a[10];
int *p1 = &a[2];
int *p2 = &a[7];
long diff = p2 - p1;    // 5 (количество ЭЛЕМЕНТОВ, не байт)
```

⚠️ Разность имеет смысл, только если указатели указывают **на один массив**.

---

## 🔹 Константные указатели

| Запись | Что нельзя менять |
|--------|-------------------|
| `const int *p` | `*p` (значение) |
| `int * const p` | `p` (адрес) |
| `const int * const p` | И то, и другое |

**Пример:**

```c
const int *p1;         // *p1 = 5 — ошибка
int * const p2 = &x;   // p2 = &y — ошибка
```

---

## 🔹 Частые ошибки

| Ошибка | Что не так |
|--------|-----------|
| `int *p; *p = 5;` | Указатель не инициализирован |
| `p = 5;` | Присваивание числа указателю |
| `*p = &x;` | Разыменование и присваивание адреса |
| `p + p` | Сложение указателей |
| `p * 2` | Умножение указателя |
| Сравнение `p1 < p2` для разных массивов | UB |
| `void *p; p++;` | Арифметика с `void *` запрещена |
| Забыли `&` в `scanf` | `scanf("%d", &x)` |
| Лишний `&` в `printf` | `printf("%d", x)` |

---

## 🔹 Быстрые примеры

**Обход массива через указатель:**

```c
int a[5] = {1, 2, 3, 4, 5};
for (int *p = a; p < a + 5; p++) {
    printf("%d ", *p);
}
```

**Сумма элементов:**

```c
int sum(int *arr, int n) {
    int s = 0;
    for (int i = 0; i < n; i++)
        s += arr[i];       // или *(arr + i)
    return s;
}
```

**Реверс строки:**

```c
void reverse(char *s) {
    char *end = s;
    while (*end) end++;
    end--;
    while (s < end) {
        char t = *s;
        *s++ = *end;
        *end-- = t;
    }
}
```

---

## 🔹 Шпаргалка-сводка

| Что | Как |
|-----|-----|
| Взять адрес | `&x` |
| Разыменовать | `*p` |
| Сдвиг вперёд | `p + n`, `p++` |
| Сдвиг назад | `p - n`, `p--` |
| Разность | `p2 - p1` (в элементах) |
| Элемент массива | `a[i]` ≡ `*(a + i)` |
| Инкремент значения | `(*p)++` |
| Изменить указатель в функции | `int **pp` |
| Константный указатель | `int * const p` |
| Указатель на константу | `const int *p` |
| Проверка перед использованием | `if (p != NULL)` |

---

**Главное правило:** арифметика указателей работает **в элементах**, а не в байтах. Компилятор сам умножает на `sizeof(тип)`. Если помнить это — большинство ошибок исчезнет.

