# Шпаргалка: функции ввода-вывода в C

Компактный справочник по двум уровням: высокоуровневому (`FILE *`) и низкоуровневому (дескрипторы).

---

## 🔹 Два уровня

| Уровень | Тип | Заголовок | Буфер |
|---------|-----|-----------|:-----:|
| Высокий | `FILE *` | `<stdio.h>` | ✅ |
| Низкий | `int` (дескриптор) | `<fcntl.h>`, `<unistd.h>` | ❌ |

---

## 🔹 Открытие и закрытие

### Высокий уровень

| Функция | Что делает |
|---------|-----------|
| `FILE *fopen(name, mode)` | Открывает файл, возвращает `FILE *` или `NULL` |
| `int fclose(FILE *f)` | Закрывает, сбрасывает буфер, возвращает 0 или `EOF` |

**Режимы:**

| Режим | Что делает |
|-------|-----------|
| `"r"` | Чтение (файл должен быть) |
| `"w"` | Запись (создаёт/очищает) |
| `"a"` | Дозапись в конец |
| `"r+"` | Чтение + запись |
| `"w+"` | Чтение + запись (очищает) |
| `"a+"` | Чтение + дозапись |
| `"rb"`, `"wb"` | Бинарные режимы |

**Пример:**

```c
FILE *f = fopen("data.txt", "r");
if (f == NULL) { perror("fopen"); return 1; }
// ...
fclose(f);
```

### Низкий уровень

| Функция | Что делает |
|---------|-----------|
| `int open(path, flags, mode)` | Открывает, возвращает дескриптор или `-1` |
| `int close(int fd)` | Закрывает дескриптор |

**Флаги:**

| Флаг | Значение |
|------|----------|
| `O_RDONLY` | Только чтение |
| `O_WRONLY` | Только запись |
| `O_RDWR` | Чтение + запись |
| `O_CREAT` | Создать, если нет |
| `O_TRUNC` | Очистить |
| `O_APPEND` | Дозапись в конец |

**Пример:**

```c
int fd = open("data.txt", O_RDONLY);
if (fd == -1) { perror("open"); return 1; }
// ...
close(fd);
```

---

## 🔹 Стандартные потоки

| Поток | Дескриптор | Назначение |
|-------|:----------:|------------|
| `stdin` | 0 | Стандартный ввод |
| `stdout` | 1 | Стандартный вывод |
| `stderr` | 2 | Вывод ошибок |

Открыты **всегда**, закрывать не нужно.

---

## 🔹 Чтение

### Высокий уровень

| Функция | Что читает | Возврат |
|---------|-----------|---------|
| `fgetc(f)` | 1 символ | `int` или `EOF` |
| `fgets(buf, n, f)` | строку до `\n` или `n-1` | `buf` или `NULL` |
| `fscanf(f, fmt, ...)` | по формату | число прочитанных |
| `fread(buf, size, n, f)` | блок байт | число прочитанных |

### Низкий уровень

| Функция | Что читает | Возврат |
|---------|-----------|---------|
| `read(fd, buf, n)` | `n` байт | число прочитанных или `-1` |

**Пример:**

```c
char buf[1024];
ssize_t n = read(fd, buf, sizeof(buf));
if (n == -1) { perror("read"); }
```

---

## 🔹 Запись

### Высокий уровень

| Функция | Что пишет |
|---------|-----------|
| `fputc(c, f)` | 1 символ |
| `fputs(s, f)` | строку |
| `fprintf(f, fmt, ...)` | по формату |
| `fwrite(buf, size, n, f)` | блок байт |

### Низкий уровень

| Функция | Что пишет |
|---------|-----------|
| `write(fd, buf, n)` | `n` байт |

**Пример:**

```c
const char *msg = "Hello\n";
write(STDOUT_FILENO, msg, strlen(msg));
```

---

## 🔹 Позиционирование

### Высокий уровень

| Функция | Что делает |
|---------|-----------|
| `fseek(f, offset, whence)` | Перемещает позицию |
| `ftell(f)` | Возвращает текущую позицию |
| `rewind(f)` | В начало |
| `feof(f)` | Проверка конца файла |

**`whence`:**

| Константа | Откуда считать |
|-----------|----------------|
| `SEEK_SET` | От начала |
| `SEEK_CUR` | От текущей |
| `SEEK_END` | От конца |

**Пример:**

```c
fseek(f, 0, SEEK_END);
long size = ftell(f);
rewind(f);
```

### Низкий уровень

| Функция | Что делает |
|---------|-----------|
| `lseek(fd, offset, whence)` | Перемещает позицию |

---

## 🔹 Буферизация

| Функция | Что делает |
|---------|-----------|
| `fflush(f)` | Сбрасывает буфер на диск |
| `setvbuf(f, buf, mode, size)` | Настраивает буфер |
| `fileno(f)` | Возвращает дескриптор для `FILE *` |
| `fdopen(fd, mode)` | Открывает `FILE *` по дескриптору |

**Пример:**

```c
fflush(stdout);   // принудительно вывести
```

⚠️ `fclose` вызывает `fflush` автоматически.

---

## 🔹 Проверка ошибок

| Функция | Что делает |
|---------|-----------|
| `feof(f)` | Вернул ли `EOF`? |
| `ferror(f)` | Была ли ошибка? |
| `clearerr(f)` | Сбрасывает флаги |

**Пример:**

```c
while ((c = fgetc(f)) != EOF) {
    // ...
}
if (ferror(f)) {
    perror("fgetc");
}
```

---

## 🔹 Сравнение уровней

| Критерий | `FILE *` | Дескриптор |
|----------|:--------:|:----------:|
| Буфер | ✅ | ❌ |
| Переносимость | Везде | POSIX |
| Контроль | Меньше | Полный |
| Скорость мелких операций | Быстрее | Медленнее |
| Формат | `fprintf`/`fscanf` | Нет |
| Заголовок | `<stdio.h>` | `<fcntl.h>` |

---

## 🔹 Спецификаторы `printf`/`scanf`

| Спецификатор | Тип |
|--------------|-----|
| `%d` | `int` |
| `%u` | `unsigned int` |
| `%ld` | `long` |
| `%f` | `float`, `double` |
| `%lf` | `double` (в `scanf`) |
| `%c` | `char` |
| `%s` | строка |
| `%p` | указатель |
| `%x` | hex |
| `%o` | octal |
| `%%` | `%` |

**Форматирование:**

| Код | Что делает |
|-----|-----------|
| `%5d` | ширина 5, вправо |
| `%-5d` | ширина 5, влево |
| `%05d` | нули слева |
| `%.2f` | 2 знака после точки |

---

## 🔹 Типичные ошибки

| Ошибка | Решение |
|--------|---------|
| Забыли `if (f == NULL)` | Проверять всегда |
| Забыли `fclose` | Всегда закрывать |
| `while (!feof(f))` | Использовать `fgetc`/`fgets`/`fscanf` |
| `char c` для `fgetc` | Использовать `int c` |
| `scanf("%d", x)` | Нужно `&x` |
| `printf("%d", &x)` | Лишний `&` |
| Открыть `"r"` несуществующий | Проверять `NULL` |
| `open` без `O_CREAT` | Добавить `O_CREAT` |
| Не проверить `read`/`write` на `-1` | Проверять всегда |

---

## 🔹 Шаблоны

**Чтение файла построчно:**

```c
FILE *f = fopen("file.txt", "r");
char line[256];
while (fgets(line, sizeof(line), f)) {
    printf("%s", line);
}
fclose(f);
```

**Запись в файл:**

```c
FILE *f = fopen("out.txt", "w");
fprintf(f, "Значение: %d\n", 42);
fclose(f);
```

**Чтение через дескриптор:**

```c
int fd = open("file.txt", O_RDONLY);
char buf[1024];
ssize_t n;
while ((n = read(fd, buf, sizeof(buf))) > 0) {
    write(STDOUT_FILENO, buf, n);
}
close(fd);
```

**Копирование файла:**

```c
FILE *in = fopen("a.txt", "rb");
FILE *out = fopen("b.txt", "wb");
int c;
while ((c = fgetc(in)) != EOF) {
    fputc(c, out);
}
fclose(in);
fclose(out);
```

---

## 🔹 Сводка

| Что нужно | Функция |
|-----------|---------|
| Открыть (высокий) | `fopen` |
| Закрыть (высокий) | `fclose` |
| Открыть (низкий) | `open` |
| Закрыть (низкий) | `close` |
| Читать символ | `fgetc` / `read` |
| Читать строку | `fgets` |
| Читать по формату | `fscanf` |
| Писать символ | `fputc` / `write` |
| Писать строку | `fputs` |
| Писать по формату | `fprintf` |
| Бинарный ввод-вывод | `fread` / `fwrite` |
| Позиция | `fseek` / `ftell` / `lseek` |
| Проверка конца | `feof` |
| Сброс буфера | `fflush` |

---

**Главное правило:** для **обычной работы** — `fopen`/`fclose` и функции `f*`. Для **низкоуровневого контроля** — `open`/`close` и `read`/`write`. Всегда проверяйте результат: `NULL` для `FILE *`, `-1` для дескрипторов и системных вызовов. ✅
